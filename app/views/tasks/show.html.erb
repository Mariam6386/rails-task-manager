<h1>My task: <%= @task.title %></h1>
<h2><%= @task.details %></h2>
<h3><%= @task.completed? ? "This task is completed" : "This task is not completed yet" %></h3>

<%= link_to "Back to list", tasks_path %>

<%= link_to "Edit Task", task_edit_path %>

<%= link_to 'Delete',
task_path(@task), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>
